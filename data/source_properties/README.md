Depose ici les exports de la collection Firestore `properties`.

Formats supportes:
- `properties.json`
- `properties.jsonl`
- tout fichier `.json` ou `.jsonl` contenant soit une liste d'objets, soit un objet avec `items` ou `documents`

Le pipeline garde seulement le schema metier attendu par le modele.
